<!--components/classic/essay/index.wxml-->
<view class="classic-container" hidden="{{hidden}}">
  <image src="{{img}}" class="classic-img"></image>
  <image src="images/essay@tag.png" class="classic-tag"></image>
  <text class="classic-content">{{content}}</text>
</view>
